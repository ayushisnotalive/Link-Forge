{
  "name": "link-forge-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=packages/web",
    "build": "npm run build --workspace=packages/web && mkdir -p dist && cp -r packages/web/dist/* dist/",
    "lint": "npm run lint --workspace=packages/web"
  }
}
